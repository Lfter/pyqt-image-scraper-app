{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run PyQt App",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/launch_app.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
